{% extends 'base.html.twig' %}


{% block title %}Utilisateur{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{% include "user/index.html.twig" %}


<div class="example-wrapper">
<div class="card" style="width: 18rem;">
    <div class="card-body">
            <div> Nom : {{user.nom}}</div>
            <div> Prènom : {{user.prenom}}</div>
            <div> Email : {{user.email}}</div>
            <div> Téléphone : {{user.telephone}}</div>
            <div> Code Postal : {{user.codePostal}}</div>
            <div> Ville : {{user.ville}}</div>
        <a href="{{path('modifiUser',{'id': user.id})}}" class="col btn btn-secondary btn-sm ">Modifier</a>
</div>

   
</div>

</div>
{% endblock %}

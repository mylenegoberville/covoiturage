 <!-- Navigation-->
 <style>
    a:hover{
   color:none;
   
    }
  
    a:active{
      text-decoration:underline;
    }
   </style>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Carpooling</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobileMenu" aria-controls="mobileMenu" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mobileMenu">
        <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_trajet') }}">Accueil</a>
            </li>
            {% if not app.user %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
            </li>
        {% else %}
        {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item active"><a class="nav-link" href="{{ path('app_user')}}">
            Trajet</a></li>
        <li class="nav-item active"><a class="nav-link" href="{{ path('app_user')}}">
            User</a></li>
         {% endif %}
        <li class="nav-item active"><a class="nav-link" href="{{ path('app_user')}}">
            Compte</a></li>
        <li class="nav-item active"><a class="nav-link" href="{{ path('app_logout')}}">
            Déconnection</a></li>
        {% endif %}
        </ul>
    </div>
</nav>